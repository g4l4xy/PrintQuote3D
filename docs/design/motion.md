# PQ Design · Motion

PQMotion: immediate=0ms, fast=120ms, standard=180ms, pane=240ms. Use native easing, interruptible transitions, no looping decorations. Reduce Motion disables custom transitions; native controls retain system behavior. Loading state must remain readable without animation. Keep autosave and import progress state independent of animation.
